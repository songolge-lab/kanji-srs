{
  "name": "kanji-srs",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "vite build && cd electron && npm start",
    "electron:build": "vite build && cd electron && npm run build:win"
  },
  "devDependencies": {
    "vite": "^8.0.16",
    "vite-plugin-pwa": "^1.3.0"
  }
}
